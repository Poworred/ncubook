# 此间 - 环境变量范本 (.env.example)
# 本地开发请复制为 `.env.local` 并填入实际密钥（严禁提交真实 Key 入库）

# --- 开发者运维控制台鉴权 ---
# 控制台登录密码与远程 API 鉴权 (访问 /admin 登录)
ADMIN_PASSWORD=
# 远程发布接口鉴权 Token (可选，未配置时自动复用 ADMIN_PASSWORD)
PUBLICATION_ADMIN_TOKEN=
# 命令行同步发布接口地址 (用于 node scripts/publish.ts)
PUBLICATION_ENDPOINT=http://localhost:3000/api/admin/publish-notion

# --- Supabase 数据库与存储 ---
# Supabase 项目 API 地址（如：https://xxx.supabase.co）
SUPABASE_URL=
# Supabase 服务端 Service Role 密钥（严禁暴露至前端）
SUPABASE_SERVICE_ROLE_KEY=
# 内容库运行模式（development / production）
PUBLISHED_CONTENT_ENV=development

# --- Notion 内容发布管理 ---
# Notion Integration Token (用于读取 Notion 页面节点)
NOTION_TOKEN=
# Notion 根页面 ID (Root Page ID)
NOTION_ROOT_PAGE_ID=

# --- AI 问答引擎配置 ---
# 大模型服务 API Key (严禁暴露至前端)
AI_PROVIDER_API_KEY=
# 大模型服务 Base URL (推荐：https://api.deepseek.com)
AI_PROVIDER_BASE_URL=https://api.deepseek.com
# 聊天模型名称 (推荐：deepseek-v4-flash)
AI_CHAT_MODEL=deepseek-v4-flash
# Embedding 向量模型 (可选，默认使用词法召回)
AI_EMBEDDING_MODEL=
# 回答模式 (fixture: 本地模拟 | production: 生产上线)
AI_ANSWER_MODE=fixture
# AI 请求超时时间 (单位：ms，默认 8000)
AI_REQUEST_TIMEOUT_MS=8000
# 单 IP 每分钟请求限流次数 (默认 10)
AI_RATE_LIMIT_PER_MINUTE=10

# --- 站点信息 ---
# 站点正式域名（生产必须配置，用于 sitemap / robots / OG 分享卡片的绝对链接；本地开发可留空）
SITE_URL=

# --- RAG 评测 (可选) ---
# 问答准确率评测接口 (用于 npm run eval)
ANSWER_EVAL_ENDPOINT=http://localhost:3000/api/ask

# --- 飞书用户反馈与多维表格 (可选) ---
# 飞书收集表公开分享 URL（学生端点击反馈直达，支持 URL 预填来源、页面与问题）
NEXT_PUBLIC_FEISHU_FEEDBACK_FORM_URL=https://ncuhomer.feishu.cn/share/base/form/shrcnRe1BNvn40z4qo7k3miWAN0
# 飞书多维表格后台看板 URL（管理员在控制台直接查看收集明细）
NEXT_PUBLIC_FEISHU_ADMIN_WIKI_URL=https://ncuhomer.feishu.cn/wiki/QFvewamk0i5MWvkI8zVcDxWcnPb?table=tblISr5HwymQ2YAq&view=vewW891LQV
# 飞书自定义机器人 Webhook URL（可选，收到反馈时异步推送到飞书群）
FEISHU_FEEDBACK_WEBHOOK_URL=
